// every go file needs to have a package declared
package main

// this is the standard format libary in golang
import "fmt"

// this can only be called when package is main
func main() {
    fmt.Println("hello world")
}